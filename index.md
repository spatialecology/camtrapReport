# camtrapReport

`camtrapReport` is an R package for exploring and processing camera-trap
data and for automatically generating wildlife monitoring reports.

**License:** MIT. This repository follows the standard R package
structure, with `DESCRIPTION` specifying `MIT + file LICENSE`.
