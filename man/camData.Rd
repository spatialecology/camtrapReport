% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/camData.R
\name{camData,character-method}
\alias{camData,character-method}
\alias{camData}
\title{Read camera-trap data in Camtrap DP format}
\usage{
camData(data, habitat, study_area, update, ...)
}
\arguments{
\item{data}{A character string giving the path to a Camtrap DP dataset,
provided as a ZIP file or an extracted dataset directory.}

\item{habitat}{An optional data frame containing habitat information for
camera locations. The default is \code{NULL}.}

\item{study_area}{An optional study-area boundary provided as a spatial file
path, a \code{SpatVector}, or an \code{sf} object. The default is \code{NULL}.}

\item{update}{A logical value (default \code{FALSE}) specifying whether to
recreate a previously saved \code{camReport} object.}

\item{...}{Additional arguments. These are currently reserved for future use.}
}
\value{
A \code{\link[=camReport-classes]{camReport}} object.
}
\description{
Create a \code{\link[=camReport-classes]{camReport}} object from a Camtrap DP dataset.
}
\details{
The function reads the input Camtrap DP dataset and creates a \code{camReport}
object. The resulting object contains processed camera-trap data, metadata,
summaries, report text, report modules, and data-status information used to
generate automated reports.

If habitat information is provided, it is linked to camera locations and
used in habitat-related summaries. If a study-area boundary is provided, it
is stored with the object and used in spatial summaries where relevant.

The \code{camReport} object is normally saved within the dataset directory as
\verb{__camReport_Object.rds}. A later call using the same dataset reuses the
saved object unless \code{update = TRUE}, in which case the object is recreated.
If the dataset location is not writable, a package-specific user cache
directory is used instead.
}
\examples{
example_dataset <- system.file(
  "external",
  "dataset",
  package = "camtrapReport"
)

cm <- camData(example_dataset)
cm
}
\seealso{
\code{\link[=report]{report()}}, \code{\link[=status]{status()}}, \code{\link[=info]{info()}}, \code{\link[=gui]{gui()}}
}
\concept{camtrapReport data}
\keyword{camera-trap}
\keyword{spatial}
\keyword{species}
