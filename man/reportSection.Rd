% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reportSection.R
\name{reportSection,character-method}
\alias{reportSection,character-method}
\alias{reportSection}
\title{Create a report section}
\usage{
reportSection(name, title, parent, txt, code_setting, packages, code)
}
\arguments{
\item{name}{A character string giving a unique name for the report section.}

\item{title}{A character string giving the section title displayed in the
report. The default is \code{""}.}

\item{parent}{A character string naming the parent section, or \code{NULL}
(default) for a top-level section.}

\item{txt}{A character vector or list containing text to include in the
report section. The default is \code{NULL}.}

\item{code_setting}{Optional R Markdown chunk options for the code chunk. For
example, \code{{c(echo = FALSE, results = "asis")}}. The default is \code{NULL}.}

\item{packages}{An optional character vector giving packages required by the
code chunk. The default is \code{NULL}.}

\item{code}{Optional R code supplied inside braces, for example,
\code{{ plot(1:10) }}.}
}
\value{
A \code{.textSection} object.
}
\description{
Create a report-section object that stores text and optional R code for a
section of a \code{\link[=camReport-classes]{camReport}} report. Report sections can be
added to a report to make report generation more flexible, modular, and
extensible.
}
\details{
Each report section must have a unique \code{name}. If a new section with the same
name as an existing section is added to a report, the existing section is
replaced.

A report section can contain text only, code only, or both text and code.
When the code chunk needs access to the associated \code{camReport} object, the
object can be referred to as \code{object} inside the code block.

This function is useful for creating custom report modules or extending the
default camtrapReport workflow with project-specific text, tables, figures,
or analyses.
}
\examples{
tx <- reportSection(
  name = "introduction",
  title = "Introduction",
  parent = NULL,
  txt = "This is an introduction section."
)

tx

\dontrun{
tx2 <- reportSection(
  name = "methods",
  title = "Methods",
  parent = NULL,
  txt = "This section summarises the camera deployment design.",
  code_setting = {c(echo = FALSE, results = "asis", warning = FALSE)},
  packages = c("gt", "dplyr"),
  code = {
    object$camera_setup |>
      gt::gt() |>
      gt::tab_header(
        title = gt::md("**Camera Deployment Summary**"),
        subtitle = gt::md("Details of camera deployments per year")
      )
  }
)

tx2
}
}
\seealso{
\code{\link[=camData]{camData()}}, \code{\link[=report]{report()}}, \code{\link[=updateReportSection]{updateReportSection()}}, \code{\link[=testSection]{testSection()}}

Other report sections: 
\code{\link{section_names}()},
\code{\link{testSection,.textSection-method}},
\code{\link{updateReportSection}()}
}
\concept{report sections}
