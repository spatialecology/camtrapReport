% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update.R
\name{updateReportSection}
\alias{updateReportSection}
\alias{listReportSections}
\alias{updateReportSection,camReport-method}
\alias{listReportSections,camReport-method}
\title{Update report sections}
\usage{
updateReportSection(
  x,
  section,
  text,
  title,
  code,
  code_name,
  code_setting,
  packages,
  append_text,
  append_code
)

listReportSections(x)
}
\arguments{
\item{x}{A \code{\link[=camReport-classes]{camReport}} object created by \code{\link[=camData]{camData()}}.}

\item{section}{A single character string giving the name or title of the
report section to update.}

\item{text}{An optional character value used to update the text body of the
report section. The default is \code{NULL}, which leaves the text unchanged.}

\item{title}{An optional character value giving a new title for the report
section. The default is \code{NULL}, which leaves the title unchanged.}

\item{code}{Optional R code used to update or add a code chunk. The code can
be supplied inside braces.}

\item{code_name}{An optional character string naming the R code chunk. The
default is \code{NULL}.}

\item{code_setting}{Optional R Markdown chunk options for the code chunk. The
default is \code{NULL}.}

\item{packages}{An optional character vector giving the R packages required
by the code chunk. The default is \code{NULL}.}

\item{append_text}{A logical value (default \code{FALSE}). If \code{TRUE}, the new text
is appended to the existing section text; otherwise, the existing text is
replaced.}

\item{append_code}{A logical value (default \code{FALSE}). If \code{TRUE}, the new code
is appended to the existing code chunk; otherwise, the existing code is
replaced.}
}
\value{
\code{updateReportSection()} returns the updated \code{camReport} object
invisibly. \code{listReportSections()} returns a data frame describing the
report sections attached to the object.
}
\description{
Update the content of a report section in a
\code{\link[=camReport-classes]{camReport}} object, or list the report sections currently
attached to the object.
}
\details{
Report sections can be identified using either their name or title.
\code{listReportSections()} lists the sections attached to a \code{camReport} object and
can be used to find the exact section names before updating them.

\code{updateReportSection()} is useful for adapting the default report content to
project-specific needs, including changing section titles, replacing or
appending text, and adding or updating R code chunks.
}
\examples{
\donttest{
source_dataset <- system.file(
  "external",
  "dataset",
  package = "camtrapReport"
)
example_dataset <- tempfile("camtrapReport-example-")
dir.create(example_dataset)
invisible(file.copy(
  list.files(source_dataset, full.names = TRUE),
  example_dataset,
  recursive = TRUE
))

cm <- camData(example_dataset)

# List sections currently attached to the report
section_catalog <- listReportSections(cm)
head(section_catalog)

# Update the introduction
cm <- updateReportSection(
  cm,
  section = "introduction",
  title = "Project introduction",
  text = paste(
    "This report summarises camera-trap monitoring",
    "data for the example study site."
  )
)

# Confirm the updated section title
updated_catalog <- listReportSections(cm)
updated_catalog[
  updated_catalog$name == "introduction",
  c("name", "title"),
  drop = FALSE
]

unlink(example_dataset, recursive = TRUE, force = TRUE)
}
}
\seealso{
\code{\link[=camData]{camData()}}, \code{\link[=report]{report()}}, \code{\link[=reportSection]{reportSection()}}, \code{\link[=info]{info()}}

Other report sections:
\code{\link{reportSection,character-method}},
\code{\link[=section_names]{section_names()}},
\code{\link{testSection,.textSection-method}}
}
\concept{report sections}
