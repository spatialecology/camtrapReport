% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{report}
\alias{report}
\alias{status}
\alias{report,camReport-method}
\alias{status,camReport-method}
\title{Generate ecological and data-status reports}
\usage{
report(object, filename, view, test)

status(object, filename, view)
}
\arguments{
\item{object}{A \code{\link[=camReport-classes]{camReport}} object created by
\code{\link[=camData]{camData()}}.}

\item{filename}{An optional character string giving the output filename or
file path without an extension. The default is \code{"report"} for \code{report()}
and \code{"data_status"} for \code{status()}. Relative default filenames are written
to the camera-trap data directory.}

\item{view}{A logical value (default \code{FALSE}) specifying whether the generated
HTML report is opened after rendering.}

\item{test}{A logical value (default \code{FALSE}). If \code{TRUE}, report modules are
tested when ecological report generation fails, helping identify
problematic modules.}
}
\value{
Invisibly returns the path to the generated HTML report.
}
\description{
Generate automated HTML reports from a \code{\link[=camReport-classes]{camReport}}
object. \code{report()} creates an ecological report, while \code{\link[=status]{status()}} creates a
data-status report summarising data completeness, structure, and quality.
}
\details{
Both functions create an intermediate R Markdown file and render it to HTML.
By default, \code{report()} creates \code{report.html} and \code{report.Rmd}, while
\code{status()} creates \code{data_status.html} and \code{data_status.Rmd}.

The ecological report is generated from the report modules attached to the
\code{camReport} object. The data-status report is generated from status modules
that summarise key aspects of the dataset, including spatial, temporal,
annotation, validation, and species-level information.

If \code{test = TRUE} is used with \code{report()}, the package attempts to test report
modules when rendering fails. This can help identify modules that cause
errors during report generation.
}
\examples{
\dontrun{
cm <- camData("data-folder")

# Generate ecological report
report(cm, view = TRUE)

# Generate data-status report
status(cm, view = TRUE)
}
}
\seealso{
\code{\link[=camData]{camData()}}, \code{\link[=reportSection]{reportSection()}}, \code{\link[=updateReportSection]{updateReportSection()}},
\code{\link[=section_names]{section_names()}}

Other report generation: 
\code{\link{gui,camReport-method}}
}
\concept{report generation}
