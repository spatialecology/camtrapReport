% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/info.R
\name{info,camReport-method}
\alias{info,camReport-method}
\alias{info}
\alias{info<-}
\alias{info<-,camReport-method}
\title{Get or set information in a camReport object}
\usage{
\S4method{info}{camReport}(x, name)

\S4method{info}{camReport}(x, name) <- value
}
\arguments{
\item{x}{A \code{\link[=camReport-classes]{camReport}} object created by \code{\link[=camData]{camData()}}.}

\item{name}{A character vector naming fields to retrieve, or a single
character string naming the field to update. When omitted from \code{info()},
the default report-information fields are returned.}

\item{value}{The new value to assign to the specified field.}
}
\value{
\code{info()} returns an object of class \code{camInfo} containing the requested
fields. The replacement method returns the updated \code{camReport} object
invisibly.
}
\description{
Get selected information fields from a \code{\link[=camReport-classes]{camReport}}
object or update the value of a field used in report generation.
}
\details{
A \code{camReport} object contains metadata and report information extracted from
the camera-trap dataset, such as title, subtitle, authors, institute, site
name, description, and acknowledgement text.

The \code{info} function can be used to inspect selected fields. The replacement
form, \code{info(x, name) <- value}, can be used to update report information
before generating the report.
}
\examples{
example_dataset <- system.file(
  "external",
  "dataset",
  package = "camtrapReport"
)

cm <- camData(example_dataset)

# Retrieve all report information
info(cm)

# Retrieve selected fields
selected_info <- info(
  cm,
  name = c("title", "authors", "institute")
)

selected_info

# Update individual fields
info(cm, "title") <- "Camera-trap monitoring report"
info(cm, "institute") <- "Example Wildlife Research Institute"

# Inspect the updated fields
info(
  cm,
  name = c("title", "institute")
)
}
\seealso{
\code{\link[=camData]{camData()}}, \code{\link[=report]{report()}}, \code{\link[=status]{status()}}, \code{\link[=updateReportSection]{updateReportSection()}}
}
\concept{report metadata}
