% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/testSection.R
\name{testSection,.textSection-method}
\alias{testSection,.textSection-method}
\alias{testSection}
\title{Test a report section}
\usage{
testSection(x, object, view)
}
\arguments{
\item{x}{A \code{.textSection} object, usually created with \code{\link[=reportSection]{reportSection()}}.}

\item{object}{An optional \code{\link[=camReport-classes]{camReport}} object created by
\code{\link[=camData]{camData()}}. The default is \code{NULL}.}

\item{view}{A logical value (default \code{TRUE}) specifying whether the rendered
temporary HTML file is opened after rendering.}
}
\value{
Invisibly returns the path to the rendered temporary HTML file.
}
\description{
Render a \code{.textSection} object as a temporary HTML report to check whether
the section text and code can be rendered successfully.
}
\details{
This function is mainly used to test custom report sections before they are
added to a full \code{\link[=camReport-classes]{camReport}} report. It checks whether the
section text, R Markdown chunk settings, required packages, and R code can be
rendered successfully.

The \code{object} argument is only needed when the R code in the section refers to
a \code{camReport} object.
}
\examples{
\dontshow{if (rmarkdown::pandoc_available()) withAutoprint(\{ # examplesIf}
tx <- reportSection(
  name = "introduction",
  title = "Introduction",
  parent = NULL,
  txt = "This is an introduction section.",
  code = {
    plot(1:10)
  }
)

# Render the section without opening a browser
test_file <- testSection(
  tx,
  view = FALSE
)

file.exists(test_file)

# Remove the generated HTML file
unlink(test_file)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=camData]{camData()}}, \code{\link[=reportSection]{reportSection()}}, \code{\link[=updateReportSection]{updateReportSection()}}, \code{\link[=report]{report()}}

Other report sections:
\code{\link{reportSection,character-method}},
\code{\link[=section_names]{section_names()}},
\code{\link[=updateReportSection]{updateReportSection()}}
}
\concept{report sections}
