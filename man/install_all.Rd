% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/installAll.R
\name{install_All,ANY-method}
\alias{install_All,ANY-method}
\alias{install_All}
\title{Install packages required by camtrapReport}
\usage{
install_All(pkgs = NULL, update = FALSE, github = TRUE,
  gitlab = FALSE, ...)
}
\arguments{
\item{pkgs}{An optional character vector of additional package names.
The default is \code{NULL}.}

\item{update}{A logical value. If \code{TRUE}, optional packages are
force-reinstalled. The default is \code{FALSE}.}

\item{github}{A logical value (default \code{TRUE}) specifying whether packages
configured in GitHub repositories are included.}

\item{gitlab}{A logical value (default \code{FALSE}) specifying whether packages
configured in GitLab repositories are included.}

\item{...}{Additional named arguments passed to \code{\link[pak:pkg_install]{pak::pkg_install()}},
such as \code{lib}, \code{ask}, \code{dependencies}, or \code{upgrade}.}
}
\value{
Called for its side effects. The function returns \code{NULL} invisibly.
}
\description{
Install packages required for the full camtrapReport workflow, including
packages used by optional report modules.
}
\details{
The function checks the package list used by camtrapReport and installs
packages that are not currently available. Packages configured as GitHub
or GitLab dependencies are installed from their corresponding repositories.
All package installation is handled by \code{\link[pak:pkg_install]{pak::pkg_install()}}.

Unlike a direct call to \code{pak}, \code{install_All()} discovers the packages declared
by the currently installed YAML report modules and combines them with any
additional packages supplied through \code{pkgs}. It then delegates resolution and
installation to \code{pak}.

When \code{update = TRUE}, the requested optional packages are force-reinstalled
from their configured sources using the \code{pak} \code{reinstall} parameter.
Dependencies are upgraded only when required by the requested packages,
unless \code{upgrade = TRUE} is supplied through \code{...}.
}
\examples{
# Package installation changes the user's R library, so these examples are
# deliberately restricted to interactive sessions.
if (interactive()) {
install_All()
install_All(pkgs = "readr")
install_All(update = TRUE)

# Reinstall the requested packages and also update their dependencies.
install_All(update = TRUE, upgrade = TRUE)
}
}
\seealso{
\code{\link[=camData]{camData()}}, \code{\link[=report]{report()}}, \code{\link[=status]{status()}}
}
\concept{optional dependencies}
