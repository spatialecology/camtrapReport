% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module-dependencies.R
\name{install_all}
\alias{install_all}
\title{Install all report-module dependencies}
\usage{
install_all(
  package_references = NULL,
  lib = NULL,
  upgrade = FALSE,
  ask = interactive()
)
}
\arguments{
\item{package_references}{An optional character vector of
\link[pak:pkg_install]{\code{pak} package references}.
A named element replaces the corresponding discovered package name; an
unnamed element is added to the installation request.}

\item{lib}{An optional character vector of library paths passed to
\code{\link[pak:pkg_install]{pak::pkg_install()}}.}

\item{upgrade}{A logical value. If \code{TRUE}, update the requested packages and
their dependencies. The default is \code{FALSE}.}

\item{ask}{A logical value controlling confirmation before replacing an
installed package version. The default is \code{\link[=interactive]{interactive()}}.}
}
\value{
Invisibly returns the result from \code{\link[pak:pkg_install]{pak::pkg_install()}}, or \code{NULL}
when no dependencies are declared.
}
\description{
Automatically discover packages required to render reports and dependencies
declared by active report modules, then install them with
\code{\link[pak:pkg_install]{pak::pkg_install()}}. Module dependencies are read from the \code{packages}
option in each module's R Markdown code block.
}
\details{
\code{install_all()} scans both the module registry bundled with \code{camtrapReport}
and every user-managed registry previously used with \code{\link[=add_Module]{add_Module()}}. Module
directories are registered automatically when they are used, so no path is
required when calling \code{install_all()}. The discovered package names are
passed to \code{\link[pak:pkg_install]{pak::pkg_install()}}.
\code{package_references} can provide the source of a package that cannot be
resolved by name from a configured repository. For example,
\code{c(myPackage = "owner/myPackage")} replaces the discovered package name with
its GitHub package reference.
}
\examples{
if (interactive()) {
  install_all()

  install_all(
    package_references = c(
      camtrapDensity = "MarcusRowcliffe/camtrapDensity"
    )
  )
}
}
\seealso{
\code{\link[=add_Module]{add_Module()}}, \code{\link[=list_Modules]{list_Modules()}}, \code{\link[=reportSection]{reportSection()}}

Other report modules:
\code{\link{modules}}
}
\concept{report modules}
