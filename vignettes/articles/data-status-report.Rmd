---
title: "Data Status Check"
author: "Elham Ebrahimi"
description: "A guide to the Data Status Check and the checks used to assess the quality, completeness, and consistency of camera-trap data."
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Data Status Check}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
resource_files:
  - figures/data-status/data-status-workflow.png
  - figures/data-status/leuven-data-status.png
  - figures/data-status/amsterdam-data-status.png
  - figures/data-status/luxembourg-data-status.png
  - figures/data-status/mica-data-status.png
---

## Purpose of the Data Status Check

The Data Status Check summarises the quality, completeness, and internal consistency of a camera-trap dataset before ecological reporting. It checks the main input files, metadata, deployments, observations, taxonomy, timestamps, annotations, and spatial information.

The output helps users identify missing values, duplicated records, invalid timestamps, incomplete metadata, spatial issues, and inconsistencies between deployments and observations. It is diagnostic: completing the checks does not establish that the survey design or data are suitable for a particular ecological analysis.

Figure 1 summarises the automated workflow. `camData()` reads the camera-trap dataset and prepares it for reporting. `status()` then runs the implemented checks and generates the HTML Data Status Check.

<style>
.figure .caption,
figcaption,
p.caption {
  text-align: center;
  font-size: 0.85rem;
  font-weight: 500;
  color: #555555;
}
</style>

```{r data-status-workflow, echo=FALSE, out.width="100%", fig.align="center", fig.cap="Figure 1. Workflow for generating a Data Status Check from camera-trap data using camtrapReport.", fig.alt="Workflow for generating a Data Status Check from camera-trap data using camtrapReport."}
knitr::include_graphics("figures/data-status/data-status-workflow.png")
```



```{r data-status-basic-workflow, eval=FALSE}
library(camtrapReport)

cm <- camData("path/to/camtrap-dp-dataset")
status(cm, view = TRUE)
```

These two function calls are sufficient for the standard workflow. With
`view = TRUE`, the generated report opens in the browser. `status()` also
invisibly returns the path to the HTML file.

To retain the report at a chosen location, supply a filename in an existing
output directory:

```{r data-status-output-path, eval=FALSE}
status_file <- status(cm, filename = "reports/data_status_2026", view = FALSE)
```

If `filename` is omitted, the HTML file and its intermediate R Markdown file
are written to the R session's temporary directory. Use distinct filenames
when reports from several datasets or processing dates must be retained.


<hr class="section-divider">

## Example Data Status Checks

Below are previews of Data Status Checks generated for different camera-trap monitoring projects. Click a preview to open the complete output.

<style>
.status-gallery {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 1.5rem;
  max-width: 1050px;
  margin: 1.8rem auto 2.8rem auto;
}

.status-card {
  position: relative;
  display: block;
  overflow: hidden;
  border-radius: 18px;
  background: #003b32;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.16);
  text-decoration: none;
  color: #ffffff !important;
}

.status-card img {
  width: 100%;
  height: 320px;
  object-fit: cover;
  display: block;
  transition: transform 0.25s ease, filter 0.25s ease;
}

.status-card:hover img {
  transform: scale(1.05);
  filter: brightness(1.06);
}

.status-card-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to top,
    rgba(0, 0, 0, 0.88),
    rgba(0, 0, 0, 0.42),
    rgba(0, 0, 0, 0.06)
  );
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  padding: 1.25rem;
}

.status-card .status-card-title {
  margin: 0 0 0.25rem 0;
  color: #ffffff !important;
  font-size: 1.35rem;
  line-height: 1.25;
  font-weight: 800 !important;
  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.85);
}

.status-card .status-card-text {
  margin: 0;
  color: #ffffff !important;
  font-size: 1rem;
  line-height: 1.35;
  font-weight: 700 !important;
  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.85);
}

.status-card .status-card-badge {
  display: inline-block;
  width: fit-content;
  margin-top: 0.75rem;
  padding: 0.30rem 0.80rem;
  border-radius: 999px;
  background: rgba(0, 0, 0, 0.35);
  border: 1px solid rgba(255, 255, 255, 0.75);
  color: #ffffff !important;
  font-size: 0.88rem;
  font-weight: 700 !important;
  text-shadow: 0 1px 5px rgba(0, 0, 0, 0.85);
}

.status-card:hover,
.status-card:focus,
.status-card:hover .status-card-title,
.status-card:hover .status-card-text,
.status-card:hover .status-card-badge {
  color: #ffffff !important;
  text-decoration: none;
}

@media (max-width: 800px) {
  .status-gallery {
    grid-template-columns: 1fr;
    max-width: 560px;
  }

  .status-card img {
    height: 300px;
  }
}
</style>

<div class="status-gallery">

<a class="status-card" href="https://spatialecology.github.io/camtrapReport/reports/leuven-data-status-check.html" target="_blank" rel="noopener noreferrer"><img src="figures/data-status/leuven-data-status.png" alt="Preview of the Leuven Data Status Check" loading="lazy" decoding="async"><div class="status-card-overlay"><div class="status-card-title">Leuven</div><div class="status-card-text">Data Status Check</div><span class="status-card-badge">Open output</span></div></a>

<a class="status-card" href="https://spatialecology.github.io/camtrapReport/reports/amsterdam-data-status-check.html" target="_blank" rel="noopener noreferrer"><img src="figures/data-status/amsterdam-data-status.png" alt="Preview of the Amsterdam Data Status Check" loading="lazy" decoding="async"><div class="status-card-overlay"><div class="status-card-title">Amsterdam Water Supply Dunes</div><div class="status-card-text">Data Status Check</div><span class="status-card-badge">Open output</span></div></a>

<a class="status-card" href="https://spatialecology.github.io/camtrapReport/reports/luxembourg-data-status-check.html" target="_blank" rel="noopener noreferrer"><img src="figures/data-status/luxembourg-data-status.png" alt="Preview of the Luxembourg Data Status Check" loading="lazy" decoding="async"><div class="status-card-overlay"><div class="status-card-title">Lux National Monitoring</div><div class="status-card-text">Data Status Check</div><span class="status-card-badge">Open output</span></div></a>

<a class="status-card" href="https://spatialecology.github.io/camtrapReport/reports/mica-data-status-check.html" target="_blank" rel="noopener noreferrer"><img src="figures/data-status/mica-data-status.png" alt="Preview of the MICA Data Status Check" loading="lazy" decoding="async"><div class="status-card-overlay"><div class="status-card-title">MICA Monitoring Project</div><div class="status-card-text">Data Status Check</div><span class="status-card-badge">Open output</span></div></a>

</div>

<hr class="section-divider">

## Accessing results in R

The results are also stored in the `camReport` object and can be accessed when individual tables or summaries are needed for further analysis.

```{r data-status-explore-object, eval=FALSE}
# List the available result components
names(cm$data_status)

# Inspect the species summary table
cm$data_status$Species$Table
```

For most users, the generated HTML report is the main output. Direct access through `cm$data_status` is optional.